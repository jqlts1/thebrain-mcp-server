version: '3.8'

services:
  thebrain-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      # 请替换为您的实际值
      - THEBRAIN_API_KEY=${THEBRAIN_API_KEY}
      - THEBRAIN_BRAIN_ID=${THEBRAIN_BRAIN_ID}
    restart: unless-stopped
    container_name: thebrain-mcp-server
